<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>YouTube Biru</title>
<style>
  body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(to bottom, #0d1b2a, #1e3a8a);
    color: white;
  }

  header {
    background-color: rgba(30, 58, 138, 0.9);
    padding: 20px;
    text-align: center;
  }

  header h1 {
    color: #60a5fa;
    margin:0;
    font-size:48px;
  }

  /* Menu horizontal mirip YouTube */
  .top-menu {
    display: flex;
    justify-content: center;
    background-color: #1e3a8a;
    padding: 10px 0;
    gap: 20px;
  }

  .menu-btn {
    display: flex;
    align-items: center;
    gap: 6px;
    background-color: transparent;
    border: none;
    color: white;
    font-weight: bold;
    cursor: pointer;
    padding: 8px 12px;
    border-radius: 8px;
    transition: 0.3s;
    font-size:16px;
  }

  .menu-btn:hover {
    background-color: #60a5fa;
    color: white;
  }

  .container {
    display: flex;
    justify-content: center;
    margin: 20px;
    gap: 20px;
  }

  .video-section {
    max-width: 640px;
    background-color: #1f2937;
    padding: 15px;
    border-radius: 15px;
  }

  iframe {
    width:100%;
    height:360px;
    border-radius:10px;
    border:none;
  }

  .button-container {
    display:flex;
    justify-content:center;
    margin:10px 0;
    gap:20px;
  }

  button.action-btn {
    background-color:#60a5fa;
    color:white; border:none;
    padding:10px 20px;
    margin:5px;
    border-radius:8px;
    cursor:pointer;
    font-weight:bold;
  }

  button.action-btn:hover { background-color:#93c5fd; }

  /* Sidebar History */
  .sidebar {
    width:250px;
    background-color:#1e40af;
    border-radius:12px;
    padding:15px;
    max-height:500px;
    overflow-y:auto;
    display:none;
    flex-shrink:0;
  }

  .sidebar h2 { color:#93c5fd; text-align:center; }

  .history-item {
    display:flex;
    align-items:center;
    margin-bottom:10px;
    padding:5px;
    border-radius:8px;
    cursor:pointer;
    transition:0.2s;
    background-color:#1e40af;
  }
  .history-item:hover { background-color:#60a5fa; }
  .history-item img { width:80px; height:50px; margin-right:10px; border-radius:5px; }
  .history-item a { color:white; text-decoration:none; font-weight:bold; flex:1; }

</style>
</head>
<body>

<header>
  <h1>YouTube Biru</h1>
</header>

<!-- Menu horizontal -->
<div class="top-menu">
  <button class="menu-btn" onclick="playHome()">🏠 Home</button>
  <button class="menu-btn" onclick="playTrending()">🔥 Trending</button>
  <button class="menu-btn" onclick="toggleSidebar()">📜 History</button>
</div>

<div class="container">
  <div class="video-section">
    <iframe id="mainVideo" src="https://www.youtube.com/embed/ivPQTgAz7xE" allowfullscreen></iframe>
    <div class="button-container">
      <button class="action-btn" onclick="alert('Kamu menyukai video ini 👍')">Like 👍</button>
      <button class="action-btn" onclick="alert('Link video ini siap dibagikan 🔗')">Share 🔗</button>
    </div>
  </div>

  <div class="sidebar" id="historySidebar">
    <h2>History Tontonan</h2>
    <div id="history"></div>
  </div>
</div>

<script>
  const mainVideo = document.getElementById('mainVideo');
  const historyContainer = document.getElementById('history');
  const sidebar = document.getElementById('historySidebar');

  const homeVideos = [
    { title: "Video Kamu", url: "https://www.youtube.com/embed/ivPQTgAz7xE", thumbnail: "https://img.youtube.com/vi/ivPQTgAz7xE/0.jpg" }
  ];

  const trendingVideos = [
    { title: "Trending 1", url: "https://www.youtube.com/embed/dQw4w9WgXcQ", thumbnail: "https://img.youtube.com/vi/dQw4w9WgXcQ/0.jpg" },
    { title: "Trending 2", url: "https://www.youtube.com/embed/3JZ_D3ELwOQ", thumbnail: "https://img.youtube.com/vi/3JZ_D3ELwOQ/0.jpg" }
  ];

  function addToHistory(video) {
    let history = JSON.parse(localStorage.getItem('videoHistory') || '[]');
    history = history.filter(v => v.url !== video.url);
    history.unshift(video);
    if(history.length > 5) history.pop();
    localStorage.setItem('videoHistory', JSON.stringify(history));
    renderHistory();
  }

  function renderHistory() {
    const history = JSON.parse(localStorage.getItem('videoHistory') || '[]');
    historyContainer.innerHTML = '';
    history.forEach(video => {
      const div = document.createElement('div');
      div.className='history-item';
      div.innerHTML=`<img src="${video.thumbnail}"><a href="#" onclick="playVideo('${video.url}','${video.title}','${video.thumbnail}')">${video.title}</a>`;
      historyContainer.appendChild(div);
    });
  }

  function playVideo(url, title, thumbnail){
    mainVideo.src=url;
    addToHistory({title,url,thumbnail});
  }

  function playHome() {
    const video = homeVideos[0];
    mainVideo.src = video.url;
    addToHistory(video);
  }

  function playTrending() {
    const video = trendingVideos[0];
    mainVideo.src = video.url;
    addToHistory(video);
  }

  function toggleSidebar() {
    sidebar.style.display = (sidebar.style.display==='none')?'block':'none';
  }

  addToHistory(homeVideos[0]);
</script>

</body>
</html>
